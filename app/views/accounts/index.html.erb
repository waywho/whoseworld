<p style="color: green"><%= notice %></p>

<h1>Accounts</h1>
<table class="">
    <thead>
      <tr>
        <th>Name</th>
        <th>Subdomain</th>
        <th>Domain</th>
        <th></th>
      </tr>
    </thead>
    <tbody id="accounts">
    <% @accounts&.each do |account| %>
      <%= render account %>
    <% end %>
    </tbody>
  </table>

<%= link_to "New account", new_account_path %>
