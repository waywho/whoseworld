<div id="<%= dom_id page %>">
	<% unless local_assigns[:hide_title] %>
		<h1><%= page.title %></h1>
	<% end %>
	<% page.medias.each do |media| %>
		<h3><%= media.title %></h3>
		<div class="mb-4"><%= raw media.source %></div>
	<% end %>
</div>
